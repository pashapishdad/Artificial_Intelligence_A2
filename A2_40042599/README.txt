These are the required external libraries:
	pandas
	numpy
	sklearn
	matplotlib
Install the required libraries mentioned above;
Put all the files in the same directory and then run Assignment2.py, additional files will be created in the same directory(models, vocabulary, removed words, results).
Then a plot will be shown which represents the performance based on the remaining words in the vocabulary.